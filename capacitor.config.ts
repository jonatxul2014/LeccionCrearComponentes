import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'LeccionCrearComponentes',
  webDir: 'www'
};

export default config;
